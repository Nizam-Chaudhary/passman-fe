import CreateMasterPassword from "@/components/CreateMasterPassword";

export default function MasterPassword() {
  return (
    <div className="flex justify-center items-center h-screen">
      <CreateMasterPassword />
    </div>
  );
}
